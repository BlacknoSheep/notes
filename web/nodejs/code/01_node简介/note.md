# 一、Node.js简介
- 运行在服务端的js
- 用来编写服务器
- 特点：
    - **单线程**、**异步**、**非阻塞**
    - 统一API

# 二、nvm版本管理器
命令：
    - nvm list：显示已安装的node版本
    - nvm node_mirror 镜像地址
    - nvm use 版本号：切换要使用的node版本

# 三、node.js和JavaScript区别
node中可以使用ECMAScript
但不可以使用DOM和BOM